fastapi>=0.116.2
starlette>=0.47.2
uvicorn==0.30.6
httpx==0.27.2
redis==5.0.8
# Four routes take `file: UploadFile = File(...)` — /api/upload,
# /api/audio/transcribe, /api/vision/analyze, /api/vision/presence.
# FastAPI raises at import without this, so the dashboard container
# has never started. Invisible to `scripts/test_dashboard.py` because
# CI installs every requirements.txt into one environment, where some
# other service had already brought it in. Only the container can see
# a missing per-service dependency.
python-multipart>=0.0.26
# `common/policy.py` reads security/policy.yml at import. Without
# pyyaml it falls back to a loader that cannot parse YAML, the
# policy loads empty, and every permission drops to its most
# restrictive default. Proven in CI on 2026-08-06 (tool-gate).
pyyaml>=6.0
