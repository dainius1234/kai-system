fastapi>=0.116.2
starlette>=0.47.2
uvicorn==0.34.0
pydantic>=2.11.5
cognee==1.1.3
# Cognee's chunker loads a HuggingFace tokenizer (HUGGINGFACE_TOKENIZER env
# var) via the transformers library; not pulled in transitively by cognee==1.1.3.
transformers>=4.40.0
