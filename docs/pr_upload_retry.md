# PR Upload Retry Note

This commit exists only to allow a clean PR re-send after an upload failure.

Scope remains:
- remove `scripts/rescue_pr32.sh`
- remove `docs/mobile_pr_rescue.md`
- remove `mobile-pr-rescue` target from `Makefile`
