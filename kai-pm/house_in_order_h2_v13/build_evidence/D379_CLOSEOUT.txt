D379 / D381 / D382 TRANCHE — CLOSE-OUT
EVIDENCE CLASS: PRODUCER MEASUREMENT - SIGHTED - ZERO ADMISSION WEIGHT.
Orion self-adjudication carries no admission weight and none is claimed.

THIS TRANCHE IS NOT CLOSED. The exit gate is FAIL and says so.

STATUS PROVENANCE (D386): every status below is
subprocess.CompletedProcess.returncode, read from the child that produced
the captured output in D379_CONTROLS.txt. No shell status, no pipeline
status, no parsed text, no supplied value.

  D379 control process exit status    1     (181 passed, 12 failed)
  cal_fixtures process exit status    1     SEE BELOW — not caused by this
                                            tranche
  capture parent returns              1

D379 §8 EXECUTABLE-BOUND EXECUTION — the banked matrix at the real CLIs
  executable-bound cases 27   PASS 4   FAIL 0   HELD 23

  THIS IS THE CLI-BOUND SLICE OF THE BANKED MATRIX, NOT THE WHOLE OF IT.
  Cases whose governed subject is a DECISION FUNCTION rather than a shipped
  executable stay with the function that IS their subject, and are named:
    M2-1..3    classify.lifecycle
    D14-A/B/C  passa._eligible
    SB         passa scope derivation + the D382 executed derivation
    PPOP       stage_identity.classify_origin
    STAGE_A / STDLIB  stage_identity construction
  No matrix expansion and no matrix reduction: the same banked cases, each
  routed to its own governed boundary.

  PASS  Q1a-4  run_h2_v12 refuses a Pass A made under a DIFFERENT Stage A
  PASS  86-3   qualify refuses when --manifest is omitted
  PASS  86-4   qualify refuses a manifest path that does not exist
  PASS  86-5   qualify refuses an empty/unreadable manifest
  All four are blocker-free and first-effective.

  HELD 23, by cause:
    INC-34 ungoverned runtime refused first, or forced a refusal the
    governed POSITIVE could not survive — Q1a-1,2,3,5,6,7,8,9, DEP-1,2,3,
    Q1b-1..6, 86-1
    D381 §13 Stage-A prerequisite — holdout correctly refuses a CALIBRATION
    identity — I1A-1, I1B-1,2,3,4
  Six of those (Q1a-5, Q1b-3, Q1b-4, Q1b-5) are recorded as OVER-DETERMINED:
  the intended finding IS present in the output, and so is an independent
  blocker, so rc is not attributable to the predicate without a clean
  comparator. Finding observed; CONSEQUENTIALITY NOT PROVEN.

  HELD IS NOT A PASS AND NOT A SKIP. Every HELD case fails the exit gate and
  names the predicate that could not be measured. No verdict was assigned
  from expectation: EXPECTED_RISK_OF_HOLD != HELD, EXPECTED_PASS != PASS.

  SECTION ROLL-UP IS DERIVED FROM THESE EXECUTABLE VERDICTS wherever D379
  names a shipped executable subject. A helper check can no longer make a
  section green over an executable HELD; helper checks remain as
  supplementary diagnostics only.

NOTHING IS CLOSED HERE. INC-36, INC-37 and INC-38 have repair candidates
implemented and wired on the executable path. That is an implementation
state, subject to hostile calibration and Kai adjudication. No ledger record
was edited and no incident is closed.

CAL_FIXTURES EXIT 1 — A CARRIED MACHINE-BOUND PATH DEPENDENCY, NOT THIS
TRANCHE
  cal_fixtures.py lines 35-36 hold two absolute temp paths:
      SUBJECT = /tmp/tmp.6xNl2hBs2V/subject
      HISTORY = /tmp/tmp.849NW5Ho8U/recover
  Both are ABSENT now. Both existed at 88e54cef, where the suite recorded
  79 PASS / 0 FAIL and fixture exit 0. cal_fixtures.py is BYTE-UNCHANGED by
  this tranche (empty diff against 20cf97c3).
  The container's /tmp was reclaimed across the session gap. This is the
  already-carried machine-bound path obligation manifesting, not a
  regression introduced here.
  NOT REPAIRED. Repointing those constants at a convenient path is the
  D383/D384 defect class, and no authority for it was given. Reported to
  Kai.

SECTION STATE
  M2        IMPLEMENTED   discrimination demonstrated
  D14       IMPLEMENTED   A/B/C start-bound eligibility
  PPOP      IMPLEMENTED   INC-37: origin metadata decides first; PPOP-1..14
  86        IMPLEMENTED   INC-36: the closed four-class rule, 86-C1..C10
  SB        IMPLEMENTED   D381 subject matrix + D382 executed derivation
  I1A       IMPLEMENTED   seed is the validated Stage-A identity
  I1B       IMPLEMENTED   universe is the frozen tree, multiset reconciled
  Q1b       WIRED         q1b_denominators() now runs ON THE CLI PATH and
                          its findings determine the process verdict. The
                          __main__ guard moved to the end of file, so
                          nothing the program needs is defined past its own
                          exit. INC-38 manifestation B is WIRED, not closed —
                          repair candidate subject to hostile
                          calibration and Kai adjudication.
                          Executable-bound Q1b-1 HELD, Q1b-2 HELD on INC-34.
  Q1a       HELD (Q1a-6)  Q1a-1,2,3,4,5,7,8,9 EXECUTE. Only the
                          qualifier-runtime-differs limb needs a governed
                          POSITIVE runtime identity (INC-34).
  DEP       HELD (DEP-2)  DEP-1 and DEP-3 EXECUTE. Only the governed-stdlib
                          positive needs a compliant interpreter (INC-34).
  STAGE_A   HELD          canonical-runtime positive limb only (INC-34)
  STDLIB    HELD          V2-ID-2a only (INC-34). D380-STDLIB-NEG-1 executes.

PRODUCER / QUALIFIER POPULATIONS — DERIVED INDEPENDENTLY, NOT SHARED
  producer   127 observed   123 classified   4 refused
             BUILTIN 33  FROZEN 19  H2 6  CENSUS 0  STDLIB 65
  qualifier  130 observed   125 classified   5 refused
             H2 7  CENSUS 0  GOVERNED_STDLIB 66  BUILTIN_OR_FROZEN 52
  The qualifier does NOT call producer_population(). The two counts differ
  because they are two independent observations of two different processes'
  module tables, which is the point.
  Refused by both: _distutils_hack, sitecustomize, typing.io, typing.re.
  The qualifier additionally refuses __main__, because d379_controls.py is
  under the H2 root and is NOT one of the ten Stage-A members. That is
  correct (Kai §8): the test harness is not a production producer, and the
  rule was not weakened to let it pass.

  Before this tranche the qualifier SKIPPED 73 loaded origins silently.

HELD IS NOT A PASS. Every HELD section still FAILS the exit gate. The
label exists so the registry does not describe a blocked limb as unwritten.

D385 STANDS. D380 section 7.6 is not weakened, no external-target
exception exists, no distro-specific root is admitted, and no
H2_PY_STDLIB_V2 was created. The contract was not relaxed to manufacture
a green.

INTERPRETER INVENTORY, READ-ONLY, NOTHING INSTALLED OR BUILT
  /usr/bin/python3.10   3.10.20   KNOWN-NEGATIVE
  /usr/bin/python3.11   3.11.15   KNOWN-NEGATIVE
  /usr/bin/python3.12   3.12.3    KNOWN-NEGATIVE
  /usr/bin/python3.13   3.13.12   KNOWN-NEGATIVE
  All four refuse identically: sitecustomize.py under the governed stdlib
  root resolves outside the governed root set.
  NO D380-COMPLIANT INTERPRETER IS LOCALLY AVAILABLE.

CLOSURE CANDIDATES, FOR KAI — NOT SELF-CLOSED
  INC-32  the executed derivation now produces every figure mechanically:
          491 emitted records / 480 distinct identities / 9 duplicate
          groups / 11 excess; Population A 167 consumer-relevant over
          166/272 with 168 all-kind; Population B 165 over 164/272 with
          DATE 160 and COMMIT 5; exactly two A->B movements;
          SB-SCOPE-ALL 491 comparable, 0 differences, 0 unresolved;
          SB-CORPUS-3 exact.
  INC-33  one blob-bound loader, two consumers, exact blob verified,
          79 PASS / 0 FAIL, process exit 0, historical six preserved.
  INC-35  CAPTURE-1/2/3 prove child exit 0/1/2 each recorded and returned
          unchanged, with output bound to that invocation; CAPTURE-4 proves
          a later successful command cannot overwrite an earlier status.
          The failing-control case is recorded truthfully as 1 in this run.
          The green case remains to be demonstrated when a run is green.

  Their ledger records are NOT edited. Closure is Kai's action.

STILL OPEN
  INC-34  no compliant interpreter measured
  INC-35  awaits a truthful capture of a GREEN run as well as this failing one

NO production Stage A, candidate, Pass A, classification run, Stage-B
package, holdout, blind 40, admission or freeze was created.
PR #122 remains DO NOT MERGE.
