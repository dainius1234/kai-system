STAGE 1 — EXACT-REQUEST REPLAY: ATTEMPT 3, AUTHORISED AND STARTED

ATTEMPT      : 3
Authorisation: Dainius/Kai, 2026-08-15, fresh and specific — "Stage-1
               Attempt 3 is AUTHORISED", granted on the D266
               pre-dispatch certificate. NOT a rerun of 31899571806 or
               31906667051; a new attempt with its own run identity.

Repair commit : dce597ca4d8282a4f43facd7ba6e320b33bcb194
Repair tree   : fa2160f26307b0ea9ce3eb3d0abf8c20b3877145
                (D266 — model-readiness gate and zero-response
                run-level refusal. 0 model-facing changes; the
                invocation surface is IDENTICAL to the D263 repaired
                baseline 9a53ee6, and send_once is untouched.)

Subject      : capture run 31894868473, tree
               1d79b145a04f1bc35a9bc9d0a31251153f6e6a10
Selected row : seq 2, logical_call_id a39b6669dc1240c3, attempt_index 1
               prompt d53797298bea, contract 98c57afadeae   (D259)
Model        : qwen2.5:3b
Endpoint     : http://ollama:11434/v1/chat/completions, timeout 300 s

Experiment   : D247 Stage 1, UNCHANGED. N1 = 10, sequential, direct HTTP.
               No Instructor. No validation. No retry, no reask, no
               replacement calls. Transport errors are recorded as that
               execution and are NOT replaced. The denominator does not
               shrink. Any request-side drift invalidates the whole
               experiment — it is never a 9/10 result.

Constraints  : the original captured response is not opened until all ten
               executions are complete and sealed; no model, timeout,
               schema or request parameter is altered; the ten outcomes
               are interpreted under D247's already-frozen rules and no
               interpretation is invented after seeing them.

Prerequisites, ALL of which hold before model call 1
  1. the capture artifact is present, or the run refuses;
  2. re-selection under S1 reproduces seq 2 / d53797298bea / 98c57afadeae;
  3. attempt 3's request_hash EQUALS attempt 1's, read from run
     31899571806's freeze manifest;
  4. the output path is proven writable in the same image, as the same
     `app` user, at the same container-local path;
  5. the model pull completes SUCCESSFULLY in the foreground — its exit
     status is the gate, not a container that has merely started;
  6. the EXACT frozen runtime model is proven present in the server's
     own inventory. A healthy server is insufficient. A running
     ollama-pull container is insufficient. A related tag of the same
     family is insufficient.

  Order note: the shipped order is 1-2-3-4 then 5-6, cheapest first, so
  a 1.7 GB pull is not spent before the request identity is proven. The
  operator's stated sequence listed 5-6 before 3-4. All six hold before
  model call 1 either way; the deviation is recorded here rather than
  discovered later.

  If ANY prerequisite fails the attempt STOPS BEFORE MODEL CALL 1 and
  returns an explicit refusal / STAGE 1 UNMEASURED. No rerun, no
  replacement, no reduced denominator.

Aggregate rule: zero model responses across the ten executions is
               STAGE 1 UNMEASURED and a non-green run, however complete
               the population looks. Row-level transport outcomes are
               all preserved. No threshold beyond this is invented.

Attempt 1    : run 31899571806, run_attempt 1. 0 of 10 executions.
               INSTRUMENT / INFRASTRUCTURE FAILURE — UNMEASURED.
Attempt 2    : run 31906667051, run_attempt 1, head b51c8f5. Ten
               transport attempts, 0 model responses — UNMEASURED. The
               server was healthy; the model was not there.
               Both are permanently banked. Nothing here re-labels them
               and no execution of either is replaced.

Standing record (do not collapse these two states): docker-compose.full
               .yml ALREADY declared memu-graph -> ollama-pull
               `service_completed_successfully`. The declaration was
               PRESENT but NOT ENFORCED in the execution path, because
               the replay's `--no-deps` told Compose to ignore it. A
               declared gate that is bypassed is not a missing gate; it
               is worse, because it reads as protection while not being
               in force.

Still blocked: Stage 2 remains BLOCKED and requires separate
               authorisation. Ownership remains UNMOVED. 048 C remains
               BLOCKED.

A live model executes. That is explicitly authorised for this run and
recorded here BEFORE it starts, not reconstructed afterwards (D251).

This file is the trigger. Touching it starts the experiment, so it is
not to be edited casually — a change here is an act, not an edit.
