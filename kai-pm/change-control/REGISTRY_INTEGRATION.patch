diff --git a/scripts/security/gate_registry.py b/scripts/security/gate_registry.py
index d4d3d81..07e3132 100644
--- a/scripts/security/gate_registry.py
+++ b/scripts/security/gate_registry.py
@@ -636,6 +636,93 @@ REGISTRY: Tuple[Gate, ...] = (
          # meta-check follows make targets now, so this is the answer it
          # gets rather than the one I guessed.
          in_workflows=("core-tests.yml",)),
+    # ── CAI v1.0 (CAI-BOOT-001) — Change Authority Interlock ─────────
+    # Proposed by the CAI tranche; gate_registry.py was outside its
+    # authorised surface, so these entries were supplied as a patch.
+    # No checker can be probed bare (each needs a repository and a
+    # pinned authority); each is proven by the suite that builds real
+    # signed worlds, and the suites by mutation calibration.
+    Gate(module="check_cai_authority",
+         kind=GATE,
+         summary='a CAI Work Authority is signed by the pinned key, unexpired, not HELD/REVOKED, not replayed and not stale',
+         inputs=('kai-pm/change-control/schemas/work_authority.v1.schema.json',),
+         denominator=r"kai-wa records: \d+ WA, \d+ terminal events",
+         probe=False,
+         probe_skip_reason='needs --repo, --authority-config and --now; probed by scripts/test_cai_authority.py against real signed tags',
+         proven_by="scripts/test_cai_authority.py",
+         pending_wiring="CAI leg 2: invoked by the verifier workflow once Dainius's key is pinned"),
+    Gate(module="check_cai_scope",
+         kind=GATE,
+         summary="a baseline->candidate change stays inside a Work Authority's path scope; symlinks, gitlinks and ambiguous paths refused",
+         inputs=(),
+         denominator=r"changed entries evaluated: \d+",
+         probe=False,
+         probe_skip_reason='needs --repo, --baseline, --candidate and --scope; probed by scripts/test_cai_scope.py',
+         proven_by="scripts/test_cai_scope.py",
+         pending_wiring="CAI leg 2: invoked by the verifier workflow once Dainius's key is pinned"),
+    Gate(module="check_cai_admission",
+         kind=GATE,
+         summary='the programme baseline is derived from the COMPLETE signed admission chain, never from HEAD, main or the latest tag',
+         inputs=('kai-pm/change-control/schemas/admission.v1.schema.json', 'kai-pm/change-control/schemas/ivv_evidence.v1.schema.json'),
+         denominator=r"records: \d+ WA, \d+ admissions",
+         probe=False,
+         probe_skip_reason='needs --repo and --authority-config; probed by scripts/test_cai_admission.py',
+         proven_by="scripts/test_cai_admission.py",
+         in_workflows=("cai-authority-verifier.yml",)),
+    Gate(module="check_cai_bootstrap",
+         kind=GATE,
+         summary='the proposed CAI rulesets satisfy the design invariants and never let the executor hold the authority bypass',
+         inputs=('kai-pm/change-control/rulesets/10-authority-tags-create.json', 'kai-pm/change-control/rulesets/20-authority-tags-immutable.json', 'kai-pm/change-control/rulesets/30-main-integration.json'),
+         denominator=r"design rulesets read: \d+",
+         probe=False,
+         probe_skip_reason='needs --design; probed by scripts/test_cai_bootstrap.py',
+         proven_by="scripts/test_cai_bootstrap.py",
+         in_workflows=("cai-authority-verifier.yml",)),
+    Gate(module="test_cai_authority",
+         kind=GATE,
+         summary='CAI hostile calibration A1-A9, H1-H4, N1-N3: real signed worlds, the shipped checker as a subprocess, exit code and reason',
+         inputs=(),
+         denominator=r"declared \d+, executed \d+",
+         probe=False,
+         probe_skip_reason='builds real Git repositories and OpenPGP keys; proven by scripts/test_cai_mutation.py',
+         proven_by="scripts/test_cai_mutation.py",
+         in_workflows=("cai-authority-verifier.yml",)),
+    Gate(module="test_cai_scope",
+         kind=GATE,
+         summary='CAI hostile calibration S1-S7: real signed worlds, the shipped checker as a subprocess, exit code and reason',
+         inputs=(),
+         denominator=r"declared \d+, executed \d+",
+         probe=False,
+         probe_skip_reason='builds real Git repositories and OpenPGP keys; proven by scripts/test_cai_mutation.py',
+         proven_by="scripts/test_cai_mutation.py",
+         in_workflows=("cai-authority-verifier.yml",)),
+    Gate(module="test_cai_admission",
+         kind=GATE,
+         summary='CAI hostile calibration V1-V5, D1-D8, N4, X1-X9: real signed worlds, the shipped checker as a subprocess, exit code and reason',
+         inputs=(),
+         denominator=r"declared \d+, executed \d+",
+         probe=False,
+         probe_skip_reason='builds real Git repositories and OpenPGP keys; proven by scripts/test_cai_mutation.py',
+         proven_by="scripts/test_cai_mutation.py",
+         in_workflows=("cai-authority-verifier.yml",)),
+    Gate(module="test_cai_bootstrap",
+         kind=GATE,
+         summary='CAI hostile calibration B1-B13: real signed worlds, the shipped checker as a subprocess, exit code and reason',
+         inputs=(),
+         denominator=r"declared \d+, executed \d+",
+         probe=False,
+         probe_skip_reason='builds real Git repositories and OpenPGP keys; proven by scripts/test_cai_mutation.py',
+         proven_by="scripts/test_cai_mutation.py",
+         in_workflows=("cai-authority-verifier.yml",)),
+    Gate(module="test_cai_mutation",
+         kind=GATE,
+         summary='every CAI control is killed by a hostile case when it is disabled in a throwaway copy',
+         inputs=(),
+         denominator=r"mutants declared \d+",
+         probe=False,
+         probe_skip_reason='runs every CAI suite once per mutant (minutes); its own unmutated control run must pass first',
+         proven_by="scripts/test_cai_mutation.py",
+         in_workflows=("cai-authority-verifier.yml",)),
 )
 
 BY_MODULE = {gate.module: gate for gate in REGISTRY}
