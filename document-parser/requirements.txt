fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pymupdf>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0
xlrd>=2.0.1
python-pptx>=0.6.21
ezdxf>=1.1.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# `@app.post("/parse")` takes `file: UploadFile = File(...)`,
# and FastAPI raises at import without this — so the container
# has crash-looped in every deployment. It never blocked the
# bring-up because `dashboard` waits on it with
# `condition: service_started`, which a restarting container
# satisfies. browser-agent, perception/audio, perception/vision
# and screen-capture all list it; this one was missed.
python-multipart>=0.0.26
# `common/policy.py` reads security/policy.yml at import. Without
# pyyaml it falls back to a loader that cannot parse YAML, the
# policy loads empty, and every permission drops to its most
# restrictive default. Proven in CI on 2026-08-06 (tool-gate).
pyyaml>=6.0
